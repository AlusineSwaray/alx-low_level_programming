#include <stdio.h>
/**
 * main - this is me doing a test file to prepare me for 
 *task 8 of 0x02 functions, will revise it later
 *
 * Return: 0 unless otherwise
 */
int main(void)
{
	for ( int i = 00; i <= 60; i++)
		putchar(i);
		putchar('\n');
	return (0);
}
